<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- El color primario se usa cuando el ítem está seleccionado -->
    <item android:color="@color/select" android:state_checked="true"/>
    <!-- El color gris se usa cuando el ítem no está seleccionado -->
    <item android:color="@color/un_select" android:state_checked="false"/>
</selector>